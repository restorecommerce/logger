{
  "name": "@restorecommerce/logger",
  "version": "0.1.10",
  "description": "Opinionated wrapper and configurator for the winston logging tolkit",
  "main": "index",
  "author": "Invend GmbH",
  "repository": {
    "type": "git",
    "url": "https://github.com/restorecommerce/logger.git"
  },
  "license": "MIT",
  "keywords": [
    "restore",
    "commerce",
    "logger"
  ],
  "dependencies": {
    "@restorecommerce/winston-elasticsearch-transformer": "^0.1.7",
    "cls-rtracer": "^1.2.0",
    "cls-hooked": "^4.2.2",
    "winston": "^3.2.1",
    "winston-elasticsearch": "^0.7.12"
  },
  "devDependencies": {
    "@restorecommerce/eslint-config-restorecommerce": "^0.1.1",
    "@types/mocha": "^5.2.7",
    "@types/node": "^12.6.8",
    "coveralls": "^3.0.4",
    "mocha": "^6.1.4",
    "nyc": "^14.1.1",
    "should": "^13.2.3",
    "tslint": "^5.18.0",
    "typescript": "^3.5.3"
  },
  "scripts": {
    "pretest": "npm run tsctests",
    "tsctests": "tsc -d",
    "test": "npm run lint && npm run mocha",
    "lint": "tslint './*.ts'",
    "postinstall": "tsc -d",
    "mocha": "mocha ./test/*.js --exit",
    "coveralls": "nyc --reporter text --reporter lcov npm test"
  },
  "engines": {
    "node": ">= 9.2.0"
  }
}
