{
  "name": "@restorecommerce/logger",
  "version": "0.1.2",
  "description": "Opinionated wrapper and configurator for the winston logging tolkit",
  "main": "index",
  "author": "Invend GmbH",
  "repository": {
    "type": "git",
    "url": "https://github.com/restorecommerce/logger.git"
  },
  "license": "MIT",
  "keywords": [
    "restore",
    "commerce",
    "logger"
  ],
  "dependencies": {
    "@restorecommerce/winston-elasticsearch-transformer": "^0.1.0",
    "winston": "^2.4.1",
    "winston-elasticsearch": "^0.5.8"
  },
  "devDependencies": {
    "@restorecommerce/eslint-config-restorecommerce": "^0.1.1",
    "coveralls": "^3.0.0",
    "is-generator": "^1.0.3",
    "istanbul": "^0.4.5",
    "mocha": "^5.0.5",
    "should": "^13.2.1"
  },
  "scripts": {
    "test": "npm run lint && npm run mocha",
    "lint": "eslint *.json *.js",
    "mocha": "mocha ./test/*.js --exit",
    "coveralls": "istanbul cover ./node_modules/mocha/bin/_mocha --report lcovonly -- -R spec ./test/*"
  },
  "engines": {
    "node": ">= 8.0.0"
  }
}
